import { Banner } from "./Banner";

export default function Layout() {
  /*
    Yes, I know this is a single component,
    but it's a layout component. Looks a bit cleaner.
  */
  return <Banner />;
}
